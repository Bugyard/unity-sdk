# U20 — Config validation warnings

**Status:** ✅ done · **Milestone:** M5 — Editor tooling & DX · **Depends on:** U04

## Scope
- Editor-time warnings: empty apiKey, `bc_pk_live_` key committed to source control, placeholder endpoint.

## Acceptance criteria
- Misconfiguration is flagged in the Inspector/console before a build ships.
