# U23 — PlayMode tests

**Status:** ⬜ todo · **Milestone:** M6 — Tests & QA · **Depends on:** U06

## Scope
- Test the log ring buffer bound, capture-routine ordering (overlay hidden before screenshot), Init/Shutdown lifecycle.

## Acceptance criteria
- PlayMode tests pass; buffer never exceeds `maxLogLines`.
